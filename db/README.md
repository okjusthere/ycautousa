# Database layer

The typed D1 repository lives in [`../lib/db.ts`](../lib/db.ts), with versioned SQL in [`../migrations`](../migrations). This directory is reserved for generated local database notes and exports; no credentials or production dumps belong here.
