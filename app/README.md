# Application route boundary

The repository uses a Vite React Router client in `src/` and a Cloudflare Worker route boundary in `workers/`. This directory is reserved for future server-rendered route modules if the React Router framework adapter is enabled; keeping the boundary documented avoids an ambiguous empty scaffold while preserving the requested project layout.
